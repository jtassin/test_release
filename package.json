{
  "name": "test_release",
  "version": "1.17.0",
  "main": "index.js",
  "repository": "git@github.com:jtassin/test_release.git",
  "author": "Julien TASSIN <julien.tassin.pro@gmail.com>",
  "license": "MIT",
  "scripts": {
    "prerelease": "git checkout master && git pull && git diff-index --quiet HEAD",
    "release": "git push && yarn version --minor",
    "postrelease": "auto-changelog --issue-url https://www.pivotaltracker.com/story/show/{id} && git add CHANGELOG.md && git commit -m 'update changelog' && git push origin $npm_package_version"
  },
  "devDependencies": {
    "auto-changelog": "^1.11.0"
  }
}
